<%= form_with(model: current_user.active_relationships.build, local: true) do |f| %>
<div><%= hidden_field_tag :followed_id, @current_user.id %></div>
<%= f.submit 'フォローする', class: "btn btn-dark" %>
<% end %>
